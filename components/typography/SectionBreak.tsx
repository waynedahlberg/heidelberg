/**
 * A rest between movements, marked by the hedera (❧, U+2767's upright
 * cousin U+2618/U+2766 family) — the oldest typographic ornament in
 * continuous use. Renders as an <hr> so the semantics survive.
 */
export function SectionBreak() {
  return <hr className="section-break" />;
}
